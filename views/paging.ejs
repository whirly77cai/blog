<span class="paging">
	<% if(!isFirstPage) { %>
	<span><a href="?p=<%= page-1 %>">上一页</a></span>
	<% } %>
	&nbsp;
	<% if(!isLastPage) { %>
	<span><a href="?p=<%= page+1 %>">下一页</a></span>
	<% } %>
</span>